{
  "summary": "Telegram bot crash fixes applied — 8 bugs fixed across 5 files:\n\nCRASH CAUSES IDENTIFIED & FIXED:\n\n1. BOT MODULE-LEVEL INIT CRASH (telegram_handler.py, alerts.py, jobs.py)\n   - Problem: `bot = Bot(token=settings.TELEGRAM_BOT_TOKEN)` at module level\n   - If .env token missing or invalid → ImportError → whole app crashes\n   - Fix: Lazy init with get_bot() singleton in handler, lazy inside jobs/alerts\n\n2. WEBHOOK RETURNING 500 (routes.py)\n   - Problem: Unhandled exceptions → FastAPI auto-returns 500 to Telegram\n   - 500 = Telegram keeps retrying same update for 24hr, blocking new messages\n   - Fix: try/except around JSON parse AND handle_update, always return 200\n\n3. MARKDOWN PARSE CRASH — SILENT MESSAGE LOSS (telegram_handler.py)\n   - Problem: parse_mode=\"Markdown\" fails on unescaped chars (*_`), message lost\n   - Fix: Auto-retry as plain text on TelegramError with 'parse'/'entity'/'offset'\n\n4. TYPING TASK LEAK (telegram_handler.py)\n   - Problem: Complex stop_event/cancel logic in each handler = easy to miss\n   - Fix: Centralized _run_with_typing() helper — one place, always cleaned up\n\n5. EMPTY TEXT CRASH (telegram_handler.py)\n   - Problem: message.text can be whitespace-only → route_text_message(\"\") crash\n   - Fix: text.strip() guard before routing\n\n6. MESSAGE SPLIT ON MID-WORD (telegram_handler.py)\n   - Problem: chunks = [text[i:i+4096] ...] cuts mid-word, breaks Markdown\n   - Fix: _safe_split() splits on newlines, hard-cuts only when single line > 4096\n\n7. FILE_SIZE NONE CRASH (telegram_handler.py)\n   - Problem: doc.file_size > MAX_FILE_BYTES → TypeError if file_size is None\n   - Fix: file_size = doc.file_size or 0\n\n8. STARTUP NON-FATAL ERRORS (main.py)\n   - Problem: DB/webhook/scheduler init crash → app exits, bot goes offline\n   - Fix: Each startup step in try/except — app starts even if one fails\n\nFILES CHANGED:\n- app/bot/telegram_handler.py — Rewrote with lazy bot, _run_with_typing, _safe_split\n- app/api/routes.py — Always 200, JSON parse error handled\n- app/main.py — All startup steps non-fatal\n- app/scheduler/alerts.py — Lazy Bot() inside function\n- app/scheduler/jobs.py — Lazy Bot() + all imports inside dispatcher function",
  "status": "in_progress",
  "next_steps": "1. Bot restart karein: uvicorn app.main:app --reload\n2. Telegram pe /start bhejein aur test karein\n3. Agar koi aur error aaye, logs dekhen: logs mein ab clear error messages honge\n4. Optional: .env file mein saari keys check karein (TELEGRAM_BOT_TOKEN, GROQ_API_KEY, DATABASE_URL, FINNHUB_API_KEY)",
  "history": [
    "[2026-08-07 03:37 UTC] Complete project structure done. All files written and improved:\n\nSTACK: FastAPI + Groq LLM + PostgreSQL/pgvector + Telegram Bot + APScheduler\n\nFILES COMPLETE:\n- app/main.py — lifespan-based startup, CORS, webhook setup\n- app/config.py — pydantic-settings\n- app/database.py — SQLAlchemy async, User/Conversation/Alert/Document models\n- app/ai/agent.py — Groq tool-calling agent (12 tools)\n- app/ai/memory.py — pgvector semantic search memory\n- app/ai/prompts.py — system prompt, onboarding prompts, morning brief prompt\n- app/ai/tools.py — 12 tool definitions (stock, news, SEC, search, alerts, Gmail, Calendar, docs)\n- app/bot/telegram_handler.py — text/voice/document/photo handler, typing indicator, message chunking\n- app/bot/message_router.py — 5-step onboarding state machine + AI routing\n- app/bot/voice_handler.py — Groq Whisper transcription\n- app/services/finnhub_service.py — live quotes, company news, earnings calendar\n- app/services/yahoo_finance.py — fundamentals, market overview, price history\n- app/services/sec_edgar.py — EDGAR filings search + company facts\n- app/services/web_search.py — Tavily primary, DuckDuckGo fallback\n- app/services/document_service.py — PDF/DOCX extract, summarize, Q&A\n- app/services/voice_service.py — Groq Whisper + Telegram file download\n- app/services/google_service.py — Gmail search, Calendar events, OAuth\n- app/scheduler/jobs.py — morning brief (every 30min check) + alert monitor (every 5min)\n- app/scheduler/alerts.py — price_above/below/pct_change alert firing\n- app/api/routes.py — /webhook, /health, /me\n- app/api/auth.py — Google OAuth start + callback with success/error HTML\n- app/models/user_repo.py — User CRUD, watchlist, alert operations\n- Dockerfile — multi-stage, non-root user\n- docker-compose.yml — pgvector/pg16 + api service\n- setup.sh — quick local setup script\n- All __init__.py files added",
    "[2026-08-09 08:29 UTC] Telegram bot crash fixes applied — 8 bugs fixed across 5 files:\n\nCRASH CAUSES IDENTIFIED & FIXED:\n\n1. BOT MODULE-LEVEL INIT CRASH (telegram_handler.py, alerts.py, jobs.py)\n   - Problem: `bot = Bot(token=settings.TELEGRAM_BOT_TOKEN)` at module level\n   - If .env token missing or invalid → ImportError → whole app crashes\n   - Fix: Lazy init with get_bot() singleton in handler, lazy inside jobs/alerts\n\n2. WEBHOOK RETURNING 500 (routes.py)\n   - Problem: Unhandled exceptions → FastAPI auto-returns 500 to Telegram\n   - 500 = Telegram keeps retrying same update for 24hr, blocking new messages\n   - Fix: try/except around JSON parse AND handle_update, always return 200\n\n3. MARKDOWN PARSE CRASH — SILENT MESSAGE LOSS (telegram_handler.py)\n   - Problem: parse_mode=\"Markdown\" fails on unescaped chars (*_`), message lost\n   - Fix: Auto-retry as plain text on TelegramError with 'parse'/'entity'/'offset'\n\n4. TYPING TASK LEAK (telegram_handler.py)\n   - Problem: Complex stop_event/cancel logic in each handler = easy to miss\n   - Fix: Centralized _run_with_typing() helper — one place, always cleaned up\n\n5. EMPTY TEXT CRASH (telegram_handler.py)\n   - Problem: message.text can be whitespace-only → route_text_message(\"\") crash\n   - Fix: text.strip() guard before routing\n\n6. MESSAGE SPLIT ON MID-WORD (telegram_handler.py)\n   - Problem: chunks = [text[i:i+4096] ...] cuts mid-word, breaks Markdown\n   - Fix: _safe_split() splits on newlines, hard-cuts only when single line > 4096\n\n7. FILE_SIZE NONE CRASH (telegram_handler.py)\n   - Problem: doc.file_size > MAX_FILE_BYTES → TypeError if file_size is None\n   - Fix: file_size = doc.file_size or 0\n\n8. STARTUP NON-FATAL ERRORS (main.py)\n   - Problem: DB/webhook/scheduler init crash → app exits, bot goes offline\n   - Fix: Each startup step in try/except — app starts even if one fails\n\nFILES CHANGED:\n- app/bot/telegram_handler.py — Rewrote with lazy bot, _run_with_typing, _safe_split\n- app/api/routes.py — Always 200, JSON parse error handled\n- app/main.py — All startup steps non-fatal\n- app/scheduler/alerts.py — Lazy Bot() inside function\n- app/scheduler/jobs.py — Lazy Bot() + all imports inside dispatcher function"
  ],
  "last_updated": "2026-08-09 08:29 UTC"
}